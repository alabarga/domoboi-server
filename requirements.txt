# Core
Django==5.2.4
asgiref==3.9.0
sqlparse==0.5.3

# Database
psycopg2-binary==2.9.10

# Admin
django-unfold==0.62.0

# Static files
whitenoise==6.10.0

# Settings
python-dotenv==1.1.1

# Location field widget
django-location-field==2.7.3
six==1.17.0

# Edge device REST API
djangorestframework==3.16.1
drf-spectacular==0.28.0
inflection==0.5.1
uritemplate==4.2.0
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
referencing==0.36.2
rpds-py==0.27.1
PyYAML==6.0.2

# Push notifications
django-webpush==0.3.6

# Dev / management commands
django-extensions==4.1

# Tuya cloud bridge — used by the `tuya_poll` management command.
# Requires the companion repo to be pushed and tagged first; until then install
# it locally with:  pip install -e ../domoboi-tuya
tuya-energy @ git+https://github.com/alabarga/domoboi-tuya@v1.0.0

# HTTP client. Previously undeclared — scripts/tuya_daemon.py imports it and it
# was only present transitively via django-webpush -> pywebpush.
requests>=2.31
